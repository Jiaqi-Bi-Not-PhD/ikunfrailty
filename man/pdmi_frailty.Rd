% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{pdmi_frailty}
\alias{pdmi_frailty}
\title{Fit proposed PDMI for an ascertainment-corrected frailty model}
\usage{
pdmi_frailty(
  formula,
  data,
  kinship,
  impute,
  prior,
  M = 20,
  B = 50,
  numit = 10,
  report = c("both", "parameters", "penetrance"),
  pen_grid = NULL,
  pen_ci = TRUE,
  conf.level = 0.95,
  id = "indID",
  proband = "proband",
  currentage = "currentage",
  pedigree = NULL,
  seed = NULL,
  control = pdmi_control(),
  keep_imputations = FALSE,
  progress = interactive()
)
}
\arguments{
\item{formula}{Analysis formula. The validated engine requires
\code{Surv(t0, time, status) ~ binary + continuous + cluster(family)} or
\code{Surv(time, status) ~ binary + continuous + cluster(family)}.}

\item{data}{Analysis data frame.}

\item{kinship}{Square kinship matrix for analysis records. Row/column names
should match \code{id}; otherwise dimensions must match \code{data}.}

\item{impute}{Character vector or list naming the covariate(s) subject to
missingness. Use \code{list(continuous = "newx")},
\code{list(binary = "mgene")}, or both for explicit roles. If a named
covariate has no missing values in \code{data}, it is treated as observed with
a warning; covariates with missing values must still be listed.}

\item{prior}{Working covariate prior created by \code{pdmi_prior()}.}

\item{M}{Number of imputations.}

\item{B}{Number of sampler burn-in updates before the retained update
iterations.}

\item{numit}{Number of retained within-imputation update iterations. The
V2.2 engine returns the final state after \code{B + numit} updates.}

\item{report}{One of \code{"both"}, \code{"parameters"}, or \code{"penetrance"}. This
controls default printing; fitted objects retain all engine output.}

\item{pen_grid}{Optional list overriding the penetrance grid. Recognized
elements are \code{age}/\code{ages}, \code{prs}/\code{continuous}, \code{gene}/\code{binary}, and \code{k0}.}

\item{pen_ci}{If \code{TRUE}, \code{pen_summary()} includes confidence intervals.}

\item{conf.level}{Confidence level for parameter and penetrance intervals.}

\item{id}{Individual id column.}

\item{proband}{Proband indicator column.}

\item{currentage}{Current age column used by ascertainment correction.}

\item{pedigree}{Optional pedigree data. For binary or joint missingness it
should contain ids, family ids, and preferably \code{fatherID}/\code{motherID}.}

\item{seed}{Optional random seed.}

\item{control}{Control list from \code{pdmi_control()}.}

\item{keep_imputations}{If \code{TRUE}, retain completed imputed data sets and
sampler arrays in the fitted object.}

\item{progress}{If \code{TRUE}, print progress bars for the PDMI sampler and
final completed-data frailtypack fits. Defaults to \code{interactive()}.}
}
\value{
A \code{pdmi_frailty} object with pooled parameter estimates,
penetrance estimates, diagnostics, and the derived imputation model.
}
\description{
\code{pdmi_frailty()} is the main package entry point. Users provide the
frailtypack-style analysis formula, data, kinship matrix, missing covariates,
and working covariate prior. The function derives the technical-note
congenial imputation kernel automatically and runs the proposed V2.2
posterior-draw MI engine.
}
\examples{
\dontrun{
data("ikun_example_joint_mar20", package = "ikunfrailty")
data("ikun_example_kinship", package = "ikunfrailty")
data("ikun_example_pedigree", package = "ikunfrailty")

prior <- pdmi_prior(
  continuous = list(newx = normal_kinship(~ mgene, covariance = "kinship+iid")),
  binary = list(mgene = carrier_hwe(q = "estimate"))
)

fit <- pdmi_frailty(
  survival::Surv(t0, time, status) ~ mgene + newx + survival::cluster(famID),
  data = ikun_example_joint_mar20,
  kinship = ikun_example_kinship,
  impute = list(continuous = "newx", binary = "mgene"),
  prior = prior,
  M = 20,
  B = 50,
  numit = 10,
  pedigree = ikun_example_pedigree,
  progress = TRUE
)
summary(fit)
pen_summary(fit)
pen_plot(fit)
}
}
